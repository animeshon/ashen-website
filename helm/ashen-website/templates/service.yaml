apiVersion: v1
kind: Service
metadata:
  name: {{ include "ashen-website.fullname" . }}
  labels:
    {{- include "ashen-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ashen-website.selectorLabels" . | nindent 4 }}
