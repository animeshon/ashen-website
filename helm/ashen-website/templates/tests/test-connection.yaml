apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ashen-website.fullname" . }}-test-connection"
  labels:
    {{- include "ashen-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ashen-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
